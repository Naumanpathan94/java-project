pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
        stage('Deploy') {
            steps {
                // Add deployment steps here if needed
                echo 'Deployment stage'
            }
        }
    }
    post {
        failure {
            mail to: 'belivenot80@gmai.com',
                 subject: 'Pipeline failed',
                 body: 'Check the Jenkins console output for details.'
        }
    }
}
